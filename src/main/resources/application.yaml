spring:
  application:
    name: wms
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: sa@123
    url: jdbc:mysql://127.0.0.1:3306/wms?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
server:
  port: 8081
  servlet:
    context-path: /wms
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
#  global-config:
#    field-strategy: 0
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl